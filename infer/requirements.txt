# Pinned, because an unpinned onnxruntime is how a container that worked last week
# stops fitting in its memory limit without a single line of our code changing.
rembg==2.0.59
onnxruntime==1.20.1
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pillow==11.1.0
